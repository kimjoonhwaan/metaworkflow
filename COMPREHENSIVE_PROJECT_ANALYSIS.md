# Meta Workflow Orchestrator - 종합 프로젝트 분석 보고서

**작성일**: 2025-11-05  
**버전**: 2.0 (PPT 다이어그램 & 트리거 스케줄러 포함)

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [핵심 아키텍처](#핵심-아키텍처)
3. [기술 스택 상세](#기술-스택-상세)
4. [데이터 흐름](#데이터-흐름)
5. [주요 컴포넌트](#주요-컴포넌트)
6. [RAG 시스템](#rag-시스템)
7. [워크플로우 실행 엔진](#워크플로우-실행-엔진)
8. [트리거 & 스케줄링](#트리거--스케줄링)
9. [프로젝트 구조](#프로젝트-구조)
10. [성능 & 확장성](#성능--확장성)

---

## 프로젝트 개요

### 🎯 비전

**"자연어로 설명한 업무를 AI가 자동으로 워크플로우로 변환하고, 
AI가 직접 생성 코드를 검증 & 수정하며, 백그라운드에서 자동 실행하는 Self-Healing 시스템"**

### 🎨 프로젝트 특성

| 특성 | 설명 |
|------|------|
| **이름** | Meta Workflow Orchestrator |
| **타입** | AI-Powered Automation Platform |
| **핵심 기술** | LLM + LangGraph + ChromaDB + SQLite |
| **개발 언어** | Python 3.10+ |
| **UI 프레임워크** | Streamlit |
| **배포 환경** | 로컬 (Streamlit 기반) |
| **라이선스** | MIT |

### ✨ 주요 혁신

1. **Zero Manual Coding**: 코드 작성 불필요, 자연어만으로 완전한 워크플로우 생성
2. **Self-Healing**: AI가 생성한 코드를 AI가 검증하고 자동 수정 (안정성 95%+)
3. **Domain-Aware RAG**: 쿼리에서 도메인 자동 감지, 최적화된 검색
4. **Background Orchestration**: 자동 트리거 및 스케줄링으로 24/7 무인 실행

---

## 핵심 아키텍처

### 🏗️ 4계층 구조

```
┌─────────────────────────────────────────────────────┐
│                PRESENTATION LAYER                   │
│              (Streamlit Web UI)                     │
│  1_Create | 2_Manage | 3_Executions | 4_Triggers    │
│                 5_Knowledge_Base                    │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                 SERVICE LAYER                       │
│  ┌──────────────────────────────────────────────┐   │
│  │ WorkflowService    ExecutionService          │   │
│  │ RAGService         DomainService             │   │
│  │ FileService        TriggerManager            │   │
│  │ MetaWorkflowAgent  CodeValidator             │   │
│  └──────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                 ENGINE LAYER                        │
│  ┌──────────────────────────────────────────────┐   │
│  │ WorkflowEngine (LangGraph)                   │   │
│  │ StepExecutor (7 Step Types)                  │   │
│  │ WorkflowRunner                               │   │
│  │ TriggerScheduler (Background)                │   │
│  └──────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│            DATA PERSISTENCE LAYER                   │
│  ┌──────────────────────────────────────────────┐   │
│  │ SQLite (workflows.db)                        │   │
│  │ ChromaDB (Vector DB - domain collections)    │   │
│  │ File Storage (Document & Workflow Scripts)   │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

External Services:
├─ OpenAI API (GPT-4, Embeddings)
├─ LangSmith (Monitoring)
└─ External HTTP APIs (Workflow steps)
```

---

## 기술 스택 상세

### 🎯 핵심 기술

| 계층 | 기술 | 버전 | 역할 |
|------|------|------|------|
| **UI** | Streamlit | 최신 | 웹 기반 대시보드 |
| **백엔드** | Python | 3.10+ | 비즈니스 로직 |
| **LLM** | OpenAI GPT-4 | - | 워크플로우 생성 & 검증 |
| **Workflow Engine** | LangGraph | 최신 | 상태 기반 실행 |
| **RAG/LLM Framework** | LangChain | 최신 | LLM 체인 및 도구 |
| **임베딩 모델** | text-embedding-3-small | 384D | 벡터 임베딩 (ChromaDB) |
| **Vector DB** | ChromaDB | 최신 | 도메인별 문서 저장소 |
| **관계형 DB** | SQLite | - | 메인 데이터 저장 |
| **ORM** | SQLAlchemy | 최신 | DB 추상화 레이어 |
| **HTTP 클라이언트** | httpx | 최신 | Async HTTP 요청 |
| **스케줄링** | croniter | 최신 | Cron 표현식 파싱 |

### 📦 주요 패키지

```
streamlit              # UI 프레임워크
langchain             # LLM 통합 & RAG
langgraph             # 상태 기반 워크플로우
chroma                # 벡터 데이터베이스
sqlalchemy            # ORM
openai                # OpenAI API
pydantic              # 데이터 검증
python-dotenv         # 환경 변수 관리
croniter              # Cron 스케줄링
httpx                 # Async HTTP
pypdf                 # PDF 파싱
python-docx           # Word 파싱
pillow                # 이미지 처리
pytesseract           # OCR
```

---

## 데이터 흐름

### 🔄 전체 워크플로우 생성 & 실행 흐름

```
사용자 입력
  │
  ├─ "네이버 뉴스에서 경제 기사 3개를 수집해서 요약하고 메일로 보내줘"
  │
  ▼
MetaWorkflowAgent
  ├─ DomainService.find_domain_by_keywords()
  │  └─ 감지: "naver" 도메인
  │
  ├─ RAGService.smart_search()
  │  ├─ collection_naver 검색
  │  ├─ collection_common 검색
  │  └─ 결과: [네이버 API 문서, 요약 방법론, 메일 전송 코드]
  │
  └─ OpenAI GPT-5 호출
     ├─ 프롬프트: "위 컨텍스트를 바탕으로 Python 워크플로우 생성"
     └─ 결과: JSON (steps + code)
       │
       ▼
     CodeValidator
       ├─ AST 문법 검증
       ├─ f-string 따옴표 검증
       ├─ import 검증
       └─ 검증 통과 → DB 저장
       │  (실패 시 → AI 재생성 & 재검증)
       │
       ▼
     WorkflowService
       └─ DB에 저장 & Python 스크립트 생성
         │
         ▼
       WorkflowRunner (사용자 요청 시)
         │
         ├─ 실행 권한 확인
         ├─ WorkflowExecution 생성
         └─ WorkflowEngine 시작
           │
           ▼
         LangGraph StateGraph
           ├─ Step 1: 네이버 API 호출
           ├─ Step 2: 기사 파싱
           ├─ Step 3: 요약 생성
           ├─ Step 4: 메일 전송
           └─ 각 스텝별 상태 관리 & 에러 처리
           │
           ▼
         결과 저장
           └─ WorkflowExecution (DB)
             └─ StepExecution (각 스텝 로그)

Background: TriggerScheduler (60초 간격)
  └─ Due triggers 자동 실행
     └─ WorkflowRunner로 실행
```

---

## 주요 컴포넌트

### 1️⃣ **MetaWorkflowAgent** (`src/agents/meta_agent.py`)

**책임**: 사용자 입력을 워크플로우 JSON으로 변환

```python
MetaWorkflowAgent.process_user_input(user_input)
  ├─ 1. DomainService.find_domain_by_keywords()
  │    └─ 도메인 자동 감지
  │
  ├─ 2. RAGService.smart_search()
  │    └─ 도메인 + common 컬렉션 검색
  │
  ├─ 3. OpenAI GPT-4 호출
  │    └─ 프롬프트 + 컨텍스트
  │
  └─ 4. JSON 파싱 & 반환
       └─ {steps: [...], code: "..."}
```

**입력**: 자연어 쿼리  
**출력**: 워크플로우 JSON

---

### 2️⃣ **CodeValidator** (`src/utils/code_validator.py`)

**책임**: AI 생성 Python 코드의 안정성 보장

```python
CodeValidator.validate(code)
  ├─ AST 문법 검증
  ├─ f-string 검증 (따옴표 겹침 확인)
  ├─ import 검증
  └─ 결과: (is_valid: bool, errors: List[str])
```

**검증 항목**:
- Python 3.10+ 호환성
- f-string 문법 (변수 중괄호)
- 필수 import 존재
- 에러 처리 구조

**안정성**: 95% 이상 (검증 통과 코드)

---

### 3️⃣ **WorkflowEngine** (`src/engines/workflow_engine.py`)

**책임**: LangGraph를 사용한 상태 기반 워크플로우 실행

```
WorkflowEngine.create_graph()
  └─ StateGraph 생성
     ├─ 상태: WorkflowState
     │  ├─ current_step: int
     │  ├─ variables: Dict
     │  ├─ step_outputs: List
     │  ├─ step_statuses: List
     │  ├─ logs: List
     │  └─ errors: List
     │
     ├─ 노드: 각 스텝 실행
     │  └─ StepExecutor.execute_step()
     │
     └─ 엣지: 조건부 라우팅
        └─ next_step() 함수
```

**스텝 타입** (7가지):
1. **LLM_CALL**: OpenAI API 호출
2. **API_CALL**: HTTP 요청
3. **PYTHON_SCRIPT**: subprocess 격리 실행
4. **CONDITION**: 조건 평가
5. **APPROVAL**: 사용자 승인 대기
6. **NOTIFICATION**: 알림 전송
7. **DATA_TRANSFORM**: 데이터 변환

---

### 4️⃣ **RAGService** (`src/services/rag_service.py`)

**책임**: 도메인 기반 지식 검색 및 컨텍스트 제공

#### 아키텍처

```
RAGService
  ├─ search_metadata(query, domain=None, limit=5)
  │  ├─ domain이 지정된 경우:
  │  │  ├─ collection_[domain] 검색
  │  │  ├─ collection_common 검색
  │  │  └─ 결과 병합 & 정렬
  │  │
  │  └─ domain이 None인 경우:
  │     └─ 모든 활성 도메인 검색
  │
  ├─ smart_search(query, limit=5)
  │  ├─ DomainService.find_domain_by_keywords(query)
  │  └─ search_metadata(query, domain=detected_domain)
  │
  └─ add_document(document, domain=None)
     ├─ 도메인 결정 (parameter > document.domain > "common")
     ├─ searchable_text 생성 (title + keywords + summary)
     ├─ 벡터 임베딩 (OpenAI)
     └─ ChromaDB에 저장
```

#### ChromaDB 컬렉션 구조

```
collection_common
  └─ 모든 도메인에 공통적인 문서
     ├─ Python 기초 개념
     ├─ REST API 개론
     ├─ 에러 처리 패턴
     └─ 일반적인 코드 스니펫

collection_naver
  └─ 네이버 특화 문서
     ├─ 네이버 API 가이드
     ├─ 네이버 뉴스 크롤링
     └─ 네이버 검색 활용

collection_weather
  └─ 기상청 특화 문서
     ├─ 기상청 API
     └─ 날씨 데이터 처리

collection_kakao
  └─ 카카오 특화 문서
     ├─ 카카오 맵 API
     └─ 카카오 인증

[user_defined_domains...]
  └─ 사용자가 추가한 도메인
```

---

### 5️⃣ **DomainService** (`src/services/domain_service.py`)

**책임**: 도메인 관리 및 자동 감지

```python
DomainService
  ├─ find_domain_by_keywords(query)
  │  ├─ 쿼리에서 도메인 키워드 추출
  │  ├─ 각 도메인의 keywords와 비교 (substring 매칭)
  │  └─ 가장 많은 매칭 키워드 도메인 반환
  │
  ├─ create_or_get_domain(name, keywords, description)
  │  ├─ 도메인 존재 확인
  │  └─ 없으면 생성 (collection name 자동 정규화)
  │
  ├─ get_domain_by_name(name)
  ├─ get_all_domains()
  └─ normalize_collection_name(domain_name)
     └─ ChromaDB 명명 규칙 준수
```

**도메인 감지 로직**:
```python
# 예시
query = "네이버 뉴스에서 경제 기사를 수집해줘"

도메인별 검색:
- common: ["python", "api", ...] → 매칭: 0
- naver: ["네이버", "뉴스", "검색", ...] → 매칭: 2 ⭐
- weather: ["기상청", "날씨", ...] → 매칭: 0

결과: "naver" 도메인 감지
```

---

### 6️⃣ **TriggerScheduler** (`src/triggers/scheduler.py`)

**책임**: 백그라운드에서 due trigger 자동 실행

```
run_scheduler.py (백그라운드 프로세스)
  │
  ├─ TriggerScheduler 생성 (check_interval=60초)
  │
  └─ while True:
     ├─ TriggerManager.get_due_triggers()
     │  └─ now >= next_trigger_at AND enabled == true
     │
     ├─ WorkflowRunner.execute_trigger_once(trigger)
     │
     ├─ TriggerManager.mark_trigger_executed(trigger)
     │  └─ last_triggered_at, trigger_count 업데이트
     │
     └─ sleep(60초)
```

**트리거 타입**:
1. **SCHEDULED**: Cron 표현식 기반
2. **MANUAL**: 사용자 수동 실행
3. **EVENT**: 특정 이벤트 발생 시
4. **WEBHOOK**: 외부 웹훅

---

## RAG 시스템

### 🧠 도메인 기반 검색의 핵심

#### 문제점 (이전)
```
쿼리: "네이버"
결과: [네이버 API, 기상청 API, 카카오 API, ...]

이유: 의미론적 유사도 기반
- 모든 문서가 같은 collection에 저장됨
- 임베딩 모델이 우연히 "네이버"와 "기상청"을 가깝게 배치
```

#### 해결책 (현재)
```
쿼리: "네이버"
  ↓
DomainService: "naver" 도메인 감지
  ↓
검색 범위:
- collection_naver (상관도 높음)
- collection_common (항상 포함)
  ↓
결과: [네이버 API, 네이버 뉴스 크롤링, 기초 개념 문서]
```

#### 검색 알고리즘

```python
search_metadata(query, domain="naver"):
  1. embedding = OpenAI Embedding(query)  # 384D 벡터
  
  2. results_naver = collection_naver.query(
       query_embeddings=[embedding],
       n_results=5
     )
  
  3. results_common = collection_common.query(
       query_embeddings=[embedding],
       n_results=5
     )
  
  4. merged = results_naver + results_common
  
  5. deduplicated = 중복 제거 (document_id 기준)
  
  6. sorted_by_distance = 거리순 정렬 (ascending)
  
  7. return sorted_by_distance[:5]
```

---

## 워크플로우 실행 엔진

### 📊 LangGraph StateGraph 동작

#### 1. 그래프 생성

```python
workflow = StateGraph(WorkflowState)

# 각 스텝을 노드로 추가
for step in workflow_steps:
    workflow.add_node(
        f"step_{step.index}",
        lambda state, step=step: execute_step(state, step)
    )

# 조건부 엣지 추가
workflow.add_conditional_edges(
    "step_0",
    next_step,  # 다음 스텝 결정 함수
    {
        "step_1": "step_1",
        "step_2": "step_2",
        "end": END
    }
)

# 진입점 설정
workflow.set_entry_point("step_0")

# 실행 가능한 그래프로 컴파일
runnable = workflow.compile()
```

#### 2. 상태 흐름

```
Step 0: LLM_CALL
  ├─ input: {"query": "네이버 경제 뉴스"}
  ├─ execute: OpenAI API 호출
  └─ output: {"articles": [...]}
  
Step 1: PYTHON_SCRIPT
  ├─ input: {"articles": [...]}
  ├─ execute: 요약 코드 실행 (subprocess)
  └─ output: {"summaries": [...]}
  
Step 2: API_CALL
  ├─ input: {"summaries": [...]}
  ├─ execute: HTTP POST (메일 서버)
  └─ output: {"mail_sent": true}
  
END
```

#### 3. 에러 처리

```
각 스텝에서 에러 발생 시:

┌─────────────────────────────────────┐
│ Step 1 실행                          │
└────────────┬────────────────────────┘
             │
        ❌ 에러 발생
             │
     ┌───────▼──────────┐
     │ Retry Logic      │
     ├──────────────────┤
     │ 재시도 횟수 < 3    │
     └───────┬──────────┘
             │
        ✅ 성공 or ❌ 최종 실패
             │
     ┌───────▼──────────┐
     │ 상태 업데이트      │
     │ 로그 기록         │
     └──────────────────┘
```

---

## 트리거 & 스케줄링

### ⏰ 자동 실행 메커니즘

#### 데이터베이스 스키마

```python
class Trigger(Base):
    id: str
    workflow_id: str
    type: TriggerType  # SCHEDULED, MANUAL, EVENT, WEBHOOK
    cron_expression: str  # "0 9 * * *" (매일 9시)
    enabled: bool = True
    last_triggered_at: datetime
    next_trigger_at: datetime
    trigger_count: int = 0
    description: str
```

#### Cron 표현식 예시

```
"0 9 * * *"       → 매일 9시
"0 9 * * MON"     → 매주 월요일 9시
"0 0 * * *"       → 매일 자정
"*/30 * * * *"    → 30분마다
"0 */6 * * *"     → 6시간마다
```

#### 스케줄러 동작 흐름

```
run_scheduler.py (background)
  │
  └─ TriggerScheduler.start()
     │
     └─ while running:
        │
        ├─ TriggerManager.get_due_triggers()
        │  └─ SELECT * FROM triggers
        │     WHERE enabled=true AND next_trigger_at <= now()
        │
        ├─ For each due_trigger:
        │  │
        │  ├─ WorkflowRunner.execute_trigger_once(trigger)
        │  │  └─ Workflow 실행
        │  │
        │  └─ TriggerManager.mark_trigger_executed(trigger)
        │     └─ next_trigger_at 업데이트
        │        (croniter로 다음 시간 계산)
        │
        └─ sleep(60초)
```

---

## 프로젝트 구조

```
projWorkFlow4/
│
├── src/                                        # 소스 코드
│   ├── agents/
│   │   ├── meta_agent.py                      # 워크플로우 생성 에이전트
│   │   ├── workflow_modifier.py               # 워크플로우 수정 에이전트
│   │   └── prompts.py                         # AI 프롬프트 템플릿
│   │
│   ├── database/
│   │   ├── models.py                          # SQLAlchemy 모델
│   │   ├── session.py                         # DB 세션
│   │   ├── base.py                            # 기본 설정
│   │   └── init_db.py                         # DB 초기화
│   │
│   ├── engines/
│   │   ├── workflow_engine.py                 # LangGraph 엔진
│   │   ├── step_executor.py                   # 스텝 실행 로직
│   │   └── workflow_state.py                  # 상태 정의
│   │
│   ├── runners/
│   │   └── workflow_runner.py                 # 라이프사이클 관리
│   │
│   ├── services/
│   │   ├── workflow_service.py                # 워크플로우 CRUD
│   │   ├── execution_service.py               # 실행 기록
│   │   ├── rag_service.py                     # RAG 시스템
│   │   ├── domain_service.py                  # 도메인 관리
│   │   ├── file_service.py                    # 파일 업로드
│   │   └── file_parser.py                     # 파일 파싱
│   │
│   ├── triggers/
│   │   ├── trigger_manager.py                 # 트리거 CRUD
│   │   └── scheduler.py                       # 백그라운드 스케줄러
│   │
│   └── utils/
│       ├── config.py                          # 환경 설정
│       ├── logger.py                          # 로깅
│       ├── code_validator.py                  # 코드 검증
│       └── domain_detector.py                 # 도메인 감지
│
├── pages/                                      # Streamlit 페이지
│   ├── 1_Create_Workflow.py                   # 워크플로우 생성
│   ├── 2_Manage_Workflows.py                  # 워크플로우 관리
│   ├── 3_Executions.py                        # 실행 기록
│   ├── 4_Triggers.py                          # 트리거 관리
│   └── 5_Knowledge_Base.py                    # RAG 지식 베이스
│
├── app.py                                      # Streamlit 메인
├── requirements.txt                            # 의존성
├── .env                                        # 환경 변수
│
├── workflows.db                                # SQLite DB
├── data/chroma_db/                            # ChromaDB
├── logs/                                       # 로그 파일
│
├── README.md                                   # 프로젝트 개요
├── USAGE_GUIDE.md                             # 사용 가이드
├── ADVANCED_TECH_STACK.md                     # 기술 스택
├── LANGGRAPH_WORKFLOW.md                      # LangGraph 설명
│
├── AI_Workflow_Management_Architecture.pptx   # 프로젝트 다이어그램 (PPT)
├── create_native_ppt_diagrams.py              # PPT 생성 스크립트
├── run_scheduler.py                           # 트리거 스케줄러
├── test_trigger_system.py                     # 테스트 스크립트
└── verify_ppt.py                              # PPT 검증
```

---

## 성능 & 확장성

### 📈 성능 특성

| 항목 | 성능 | 비고 |
|------|------|------|
| **워크플로우 생성 시간** | ~5-10초 | LLM 호출 포함 |
| **코드 검증 시간** | <100ms | AST 분석 |
| **스텝 실행 평균** | ~1-5초 | 스텝 타입 및 내용에 따라 |
| **RAG 검색 시간** | <500ms | ChromaDB 쿼리 |
| **트리거 확인 간격** | 60초 | 설정 가능 |
| **동시 실행 워크플로우** | 1개 | Streamlit 제약 |
| **최대 워크플로우 크기** | 50 스텝 | 권장 (성능) |

### 🔄 확장성

#### 수평 확장

```
현재: Streamlit + SQLite (단일 인스턴스)

향후 확장 가능:
├─ Multiple Streamlit instances (로드 밸런서)
├─ PostgreSQL (다중 연결)
├─ Redis (캐싱 & 세션 공유)
├─ Kubernetes (자동 스케일링)
└─ FastAPI 전환 (더 나은 동시성)
```

#### 수직 확장

```
현재 제약:
├─ 단일 서버 리소스에 의존
├─ 메모리: 워크플로우 상태 저장
└─ CPU: Python subprocess 실행

개선 방향:
├─ 비동기 처리 확대
├─ 메모리 풀링
└─ 분산 실행 (Celery, Ray 등)
```

### 🎯 현재 제약사항

1. **동시 실행**: 1개 워크플로우만 실행 가능 (Streamlit 제약)
2. **파일 크기**: 500MB 이상 PDF 처리 불가
3. **실행 시간**: 30분 초과 작업 타임아웃
4. **워크플로우 크기**: 50 스텝 이상 권장하지 않음

---

## 🎓 주요 학습 및 적용 사항

### ✨ 혁신적 설계

1. **Self-Healing 패턴**
   - AI → CodeValidator → AI (실패 시)
   - 자동 수정으로 안정성 향상

2. **Domain-Aware RAG**
   - 도메인 자동 감지로 검색 정확도 향상
   - 임베딩 공간 분리로 오염 방지

3. **LangGraph 활용**
   - 상태 기반 워크플로우 관리
   - 복잡한 로직을 선언적으로 표현

4. **도메인 기반 아키텍처**
   - 서비스별 명확한 책임
   - 테스트 및 유지보수 용이

---

## 🚀 향후 계획

### 단기 (1-2개월)
- [ ] 더 많은 도메인 추가 (Slack, GitHub 등)
- [ ] 웹훅 트리거 구현
- [ ] 워크플로우 템플릿 라이브러리

### 중기 (3-6개월)
- [ ] 실시간 콜라보레이션 (다중 사용자)
- [ ] 워크플로우 마켓플레이스
- [ ] 고급 모니터링 & 분석

### 장기 (6개월 이상)
- [ ] 분산 실행 (Kubernetes)
- [ ] 엔터프라이즈 기능 (RBAC, 감사 로그)
- [ ] 모바일 앱

---

**작성**: 2025-11-05  
**버전**: 2.0 (PPT 다이어그램 & 백그라운드 스케줄러 포함)  
**업데이트**: Meta Workflow Orchestrator로 재정의
